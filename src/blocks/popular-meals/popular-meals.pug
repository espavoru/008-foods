//- Все примеси в этом файле должны начинаться c имени блока (popular-meals)

mixin popular-meals(data, mods)

  //- Принимает:
  //-   data    {object} - данные
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +popular-meals('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' popular-meals--' + modsList[i].trim();
      }
    }

  section.popular-meals(class=allMods)&attributes(attributes)
    block
